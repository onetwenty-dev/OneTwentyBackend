{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: FastAPI (backend_python)",
            "type": "debugpy",
            "request": "launch",
            "python": "${command:python.interpreterPath}",
            "module": "uvicorn",
            "cwd": "${workspaceFolder}",
            "args": [
                "main:app",
                "--reload"
            ],
            "jinja": true,
            "justMyCode": true
        }
    ]
}